// Root layout — minimal, just renders children
// The locale layout handles fonts and metadata
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
